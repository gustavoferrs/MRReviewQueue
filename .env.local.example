# Vercel KV credentials (obtenha no dashboard da Vercel após criar o KV store)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Senha do painel admin (server-side — usada para validar o header x-admin-password)
ADMIN_PASSWORD=review123

# Senha do painel admin (client-side — usada apenas para validação local no browser)
# Deve ser igual a ADMIN_PASSWORD
NEXT_PUBLIC_ADMIN_PASSWORD=review123
